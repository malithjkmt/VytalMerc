import { ImageURISource } from 'react-native';

export class Icons {
  static readonly backIcon: ImageURISource = require('./icons/back.png');
  static readonly closeIcon: ImageURISource = require('./icons/close.png');
}
